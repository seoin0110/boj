#include<iostream>
#include<algorithm>
#include<vector>
#include<string>
using namespace std;
using ll = long long;



int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);

    ll t;
    cin >> t;
    for (int i = 0; i < t;i++) {
        ll a;
        ll b;
        cin >> a >> b;
        cout << "yes\n";
    }

    return 0;
}