#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(0), cout.tie(0);
	cout << fixed;
	cout.precision(10);
	int a, b;
	cin >> a >> b;
	cout << (double)a / b<<"\n";


}