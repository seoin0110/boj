#include<iostream>

using ll = long long;
using namespace std;

int main() {
	cin.tie(0), cout.tie(0);
	ios_base::sync_with_stdio(0);
	cout.precision(2);
	cout << fixed;
	double a;
	cin >> a;
	cout << a / 4;
	return 0;
}